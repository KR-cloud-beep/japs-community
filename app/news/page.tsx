"use client";
import { useEffect,useState } from "react"; import Link from "next/link"; import { supabase } from "../../lib/supabase";
export default function News(){const [items,setItems]=useState<any[]>([]); useEffect(()=>{if(supabase)supabase.from("announcements").select("id,title,content,created_at").order("created_at",{ascending:false}).limit(50).then(({data})=>setItems(data??[]));},[]); return <main><header className="topbar"><div className="nav-inner"><Link className="brand" href="/japs"><span className="brand-mark">J</span> JAPS</Link><nav><Link href="/japs">공식 홈페이지</Link><Link href="/">커뮤니티</Link></nav></div></header><div className="page-wrap"><div className="section-head"><div><span className="eyebrow">JAPS NEWS</span><h1>📢 새소식</h1></div></div>{items.length===0?<div className="empty">등록된 새소식이 없습니다.</div>:items.map(x=><article className="detail-card" key={x.id}><span className="eyebrow">{new Date(x.created_at).toLocaleDateString("ko-KR")}</span><h2>{x.title}</h2><p className="detail-content">{x.content}</p></article>)}</div></main>}
